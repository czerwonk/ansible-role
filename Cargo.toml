[package]
name = "ansible-role"
version = "0.5.0"
edition = "2024"

# anyhow: application-level error handling
# serde + serde_yaml: YAML serialization for playbook generation
[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
